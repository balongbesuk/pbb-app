# Use official npm registry for security audit (mirror doesn't support audit endpoint)
# Note: For npm v10+, audit uses the configured registry automatically.
# This ensures `npm audit` hits the official registry even when using a mirror.
registry=https://registry.npmmirror.com
//registry.npmjs.org/:_authToken=
